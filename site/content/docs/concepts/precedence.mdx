---
title: Source precedence
description: Typespun resolves every configuration leaf independently across five source kinds — typed overrides, an environment source, dotenv files, compiled defaults and inline defaults — and picks the highest defined candidate before coercion.
---

Typespun resolves each leaf independently. It selects the highest-precedence
defined candidate before coercion, so an invalid lower source cannot break a
valid higher source.

<Figure
  src="/source-precedence.svg"
  alt="Source precedence from inline defaults through typed overrides"
  caption="Five layers, highest defined value wins."
/>

Text equivalent, highest to lowest:

1. typed `overrides`
2. explicit `source`; if omitted, ambient `process.env`
3. dotenv files, with later array entries winning
4. compiled JSON/YAML defaults
5. inline declaration defaults

```ts
const config = loadConfig({
  envFiles: ['.env', '.env.local'],
  source: process.env,
  overrides: { server: { port: 5000 } },
});
```

Here the override wins for `server.port`; `process.env` wins for other keys it
defines; `.env.local` wins over `.env`; then compiled and inline defaults fill
remaining leaves.

`undefined` means "no candidate." An empty string is a candidate and is
validated for its field type. `source: {}` disables ambient environment input.
Empty override objects are merge no-ops; unknown or cyclic override paths are
reported as issues.

## Related

<Cards>
  <Card
    title="Generated loader API"
    href="/docs/reference/generated-loader"
    description="envFiles, source and overrides in full, including what each one defaults to."
  />
  <Card
    title="Validation and redaction"
    href="/docs/concepts/secrets"
    description="How a selected candidate is coerced, and what an invalid one reports."
  />
</Cards>
