{
  "name": "typespun-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/omkar273/typespun.git"
  },
  "homepage": "https://github.com/omkar273/typespun#readme",
  "bugs": "https://github.com/omkar273/typespun/issues",
  "packageManager": "bun@1.4.1",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter typespun build && bun run --filter typespun-codegen build",
    "check": "bun run build && bun run format:check && bun run lint && bun run typecheck && vitest run --coverage && bun run config:check",
    "config:check": "bun run --filter 'typespun-example-*' config:check",
    "config:generate": "bun run --filter 'typespun-example-*' config:generate",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "test": "bun run build && bun run test:unit && bun run test:package",
    "test:coverage": "bun run build && vitest run --coverage",
    "test:package": "vitest run tests/",
    "test:unit": "vitest run packages/ --passWithNoTests",
    "typecheck": "bun run --workspaces --if-present typecheck"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "@vitest/coverage-v8": "5.0.1",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
